import { Meta } from '@storybook/react';
import Grid from '.';
import SvgDecorator from '../SvgDecorator';

export { Grid };

export default {
  title: 'Grid',
  component: Grid,
  decorators: SvgDecorator,
} as Meta;
